interface:
  display_name: "会战进度截图"
  short_description: "维护进度截图自动化、异步图片回传和单任务锁。"
  default_prompt: "Use $nikke-guild-progress-capture to maintain the guild progress screenshot automation skill."
policy:
  allow_implicit_invocation: true
